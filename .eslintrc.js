module.exports = {
  root: true,
  // This tells ESLint to load the config from the package `eslint-config-cdk-inception`
  // which can be found in the workspace. It is at `packages/eslint-config`.
  extends: ["eslint-config-cdk-inception"],
};
